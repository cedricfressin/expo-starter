# App home — Mobile (iOS & Android)
# Verifies the main app screen renders after navigating via deep link.
#
# Selector strategy: accessibilityLabel > visible text > testID (last resort)

appId: ${APP_ID}
tags:
  - smoke
  - feature:app
  - platform:mobile
---
- launchApp:
    clearState: true

# Navigate to app via deep link
- openLink: ${APP_ID}://app

# App home content
- assertVisible: "App"
